# Tooling scripts

Place one-off helpers (spreadsheet exporters, sanity checks against `data/samples/` schemas, ZIP bundlers, etc.) in this folder.

Prefer clear names like `dump_quest_sheet_to_json.ps1` and document prerequisites at the top of each script file.
