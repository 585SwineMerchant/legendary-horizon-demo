/**
 * LookupService — central registry accessors (realm_id, guild HQ, taxonomy tags).
 */

function getRealmRegistryRow_(realmId) {
  Logger.log('LookupService.getRealmRegistryRow_ placeholder for id=' + realmId);
  return null;
}

function listCareerClusters_() {
  return [];
}
