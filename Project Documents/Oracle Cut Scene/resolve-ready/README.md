# Oracle cutscene — Resolve-ready assets (v2)

Generated by `Codex/frontend/scripts/prepare-oracle-cutscene-assets.ps1`.

## Images (1920×1080)

| File | Shot | Source |
|------|------|--------|
| `oracle_chamber_wide.png` | Shot 1 — The Chamber | `oracle_cutscene_individuals/01_*` |
| `oracle_scroll_closeup.png` | Shot 2 — Scroll | Canon rune HTML **or** legacy AI still |
| `oracle_statue_glow.png` | Shot 3 — Oracle Awakens | `04_oracle_statue_awaken.png` |
| `oracle_vision_threads.png` | Shot 4a | `05_vision_threads.png` |
| `oracle_vision_glimpse.png` | Shot 4b | `06_vision_glimpse.png` |
| `oracle_convergence_tome.png` | Shot 5 | `07_convergence_tome.png` |

### IMG-02 (scroll) — important

The legacy AI still (`02_scroll_closeup.png`) uses **Elder Futhark letter shapes**, not the in-game guild runes.

For v2 compliance, use **`oracle-scroll-closeup-reference.html`** (paths from `guildRunes.ts`) and re-run the prepare script so headless Edge/Chrome writes `_rendered_oracle_scroll_closeup.png`.

Optional: composite the rendered runes onto the AI parchment in Photoshop/Resolve if you want more texture than the HTML still alone.

## Audio

| File | Use |
|------|-----|
| `audio/oracle_ambient_base.mp3` | Under full sequence (~−12 dB) |
| `audio/oracle_music_sting.mp3` | Main sting 0:00–0:40 |
| `audio/oracle_rune_chime.mp3` | ×3 at rune ignitions (~0:06, 0:08, 0:10) |
| `audio/oracle_tremor.mp3` | Shot 3 (~0:17) |

Prototype WAVs live in `legendary_horizon_oracle_audio_pack/`. Replace with library/Suno masters when ready.

## After export

1. Export `oracle_cutscene_v1.mp4` (1920×1080, 24fps, ~40s).
2. Optional web copy: `Codex/frontend/scripts/compress-intro-video.ps1` pattern.
3. Upload to GitHub Release (same tag as intro or `oracle-media-v1`).
4. Set `VITE_LH_ORACLE_CINEMATIC_SRC` to player HTML or direct MP4 URL.

Full edit script: `Project Documents/oracle_cutscene_production_v2.md`.
